# Build output lives outside the tree (../scraplinux-build), but just in case:
*.iso
*.iso.sha256
*.scrapsz
*.tar.xz
*.tar.gz
*.tar.bz2

# Generated by branding/gen-branding.py from scraplinux-logo-master.png.
# The master and the generator are tracked; the derived set is not, except the
# assets the ISO needs, which are listed below.
branding/icons/
branding/wallpaper/
branding/misc/

# Package recipes and ports live in their own repos now:
# github.com/apiwo/scraplinux-ports (source), scraplinux-pkgs (binaries).
# Still needed locally to build - just not published here.
ports/

# Rust build output
scraps-rs/target/

# Editor and OS noise
*.swp
*~
.DS_Store
